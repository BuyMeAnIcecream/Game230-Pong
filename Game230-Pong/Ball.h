/*
#pragma once
#include <SFML/Audio.hpp>
#include "Paddle.h"
//#include "paddle_player.h"
//#include "score.h"

class Ball: public MaShape {
	

public:
	Ball(const Paddle* paddles[], int size);
	void Update(sf::RenderWindow* window);
	void Reset(sf::RenderWindow* window);
	~Ball();
	void Load(string* s);
	//Ball(int radius);
	//~Ball();
private:
	Paddle* player1;
	Paddle* player2;

	sf::SoundBuffer* buffer;
	sf::Sound* sound;
	
};
*/
